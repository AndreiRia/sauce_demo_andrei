[project]
name = "sauce-demo-andrei"
version = "0.1.0"
description = "Demo pet project"
requires-python = ">=3.12"
dependencies = [
    "allure-pytest>=2.16.0",
    "httpx>=0.28.1",
    "playwright>=1.62.0",
    "pydantic-settings>=2.15.0",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-playwright>=0.9.0",
    "pytest-xdist>=3.8.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
